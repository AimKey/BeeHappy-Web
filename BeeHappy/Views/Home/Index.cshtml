<style>
  /* shared text styles */
  p {
    font-weight: 800;
    font-size: 2.4rem;
    line-height: 1.15;
    margin: 1rem 0;
    display: inline-block;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 300% 100%; /* stretch so the blend is obvious */
    letter-spacing: 0.02em;
  }
</style>
<div class="flex flex-column gap-4 flex-wrap px-8">
  <div style="
  display: inline-block; 
  padding: 0.5rem 1rem; 
  margin: 0.5rem;
  border-radius: 0.5rem;
">
    <span style="
    display: inline-block;
    background: linear-gradient(90deg, #ff7e5f, #feb47b, #43cea2);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-weight: bold;
    font-size: 1.5rem;
  ">
      Sunset Theme
    </span>
  </div>
  <!-- Blue ↔ Orange (complementary) -->
  <p style="background-image: linear-gradient(90deg,
      #0072ff 0%,
      #00c6ff 25%,
      #ff8c00 60%,
      #ff512f 100%
    );">
    IceFire
  </p>

  <!-- Purple ↔ Yellow (complementary) -->
  <p style="background-image: linear-gradient(90deg,
      #6a00f4 0%,
      #a200ff 20%,
      #ffd200 65%,
      #ffb100 100%
    );">
    SolarBurst
  </p>

  <!-- Teal/Green ↔ Magenta (opposites on modern palettes) -->
  <p style="background-image: linear-gradient(90deg,
      #00e5a8 0%,
      #00c3ff 30%,
      #7f00ff 65%,
      #ff2da1 100%
    );">
    Aurora
  </p>

  <!-- Deep Ocean → Indigo → Opposite Pink -->
  <p style="background-image: linear-gradient(90deg,
      #00b4db 0%,
      #0083b0 35%,
      #1f005c 70%,
      #ff7eb3 100%
    );">
    OceanDeep
  </p>

  <!-- Sunset with opposing teal punch -->
  <p style="background-image: linear-gradient(90deg,
      #ff7e5f 0%,
      #feb47b 35%,
      #43cea2 70%,
      #185a9d 100%
    );">
    SunsetTeal
  </p>

  <!-- Forest greens ↔ Magenta/Purple -->
  <p style="background-image: linear-gradient(90deg,
      #11998e 0%,
      #38ef7d 32%,
      #ff3cac 72%,
      #784ba0 100%
    );">
    ForestMagenta
  </p>

  <!-- Hot Red/Pink ↔ Cyan/Teal (opposites) -->
  <p style="background-image: linear-gradient(90deg,
      #ff0062 0%,
      #ff5ea8 28%,
      #00d2ff 68%,
      #3a7bd5 100%
    );">
    NeonPop
  </p>

  <!-- Gold/Orange ↔ Royal Blue/Cyan -->
  <p style="background-image: linear-gradient(90deg,
      #fbd72b 0%,
      #f9484a 30%,
      #2e3192 68%,
      #1bffff 100%
    );">
    Eclipse
  </p>

  <!-- Candy pinks ↔ Electric blues -->
  <p style="background-image: linear-gradient(90deg,
      #ff5858 0%,
      #f857a6 30%,
      #4facfe 70%,
      #00f2fe 100%
    );">
    CandyWave
  </p>

  <!-- Orange ↔ Magenta ↔ Violet ↔ Cyan (vaporwave-ish complements) -->
  <p style="background-image: linear-gradient(90deg,
      #ff8a00 0%,
      #e52e71 28%,
      #7f00ff 65%,
      #00dbde 100%
    );">
    VaporGlow
  </p>
</div>